# 参加方法

1課題・1データ源につき1PRを基本にする。開発手順はREADMEを参照。

## 数字を追加する

出典URL、提供者、ライセンス、取得時点、観測時点、地理、単位、定義をそろえる。
取得したCSVを不変スナップショットとして保存し、パーサーと異常系テストを追加する。
欠測を0にせず、計画値・予算・決算・推計・確定値を別に扱う。

## 解説を追加する

事実・解釈・提案を分離する。主張ごとに出典の該当箇所を示す。
短い説明でも、数字の条件・留保・反証を落とさない。煽る見出しや党派的な誘導を避ける。
人口の増減だけで政策の効果を断定しない。
AIの出力は編集候補であり、一次資料ではない。政策・法的判断・対外発表には人間の承認が必要。
新しい説明や政策評価を追加する前に、レビュー契約を実際の公開経路へ組み込む（現状は数値公開経路のみ接続済み）。

## 検証

`pnpm verify` と `pnpm test:e2e` を実行する。
ブラウザでスマホ表示、キーボード操作、出典導線を確認する。
レビュー済みと記録する場合は、実際のレビュアー、対象commit、検証方法を記録する。

## 秘密・権利

個人情報・認証情報・非公開業務データをPRに含めない。
第三者の資料は公開されているだけでは再配布可能とは限らないため、利用条件を確認する。
