# 実装・公開状態

## 2026-09-06: 人口動態・年齢構成をグラフ中心に変更

- 人口動態の18区比較は0基準の横棒、年齢構成は不詳を含む100%積み上げ棒、年別・月別の推移は折れ線を先に表示します。正確な人数・割合・出典の表は各グラフの下に残しています。
- 指標・年・地域の選択とグラフを連動させ、JavaScript無効時も初期グラフを配信します。色に加えて区分番号・線種・数値代替を用意しました。追加ライブラリや外部通信はありません。
- 独立担当 `/root/chart_review` の数式・SSR/JS・CSSレビューを受け、件数と増減の注記、凡例、既存CSSとの干渉、軸位置を修正しました。重大・中程度の未解消指摘はありません。
- 夜テーマの注記コントラスト不適合はテストで検出して修正しました。PC/スマホの実画像も確認しています。市民の理解テストは未実施です。
- 最終の検証・公開版は以下に記録します。

## 2026-09-06: 人口動態・年齢構成の追加・公開

就寝中に進められる実装の委任を受け、人口動態と年齢構成を追加しました。詳細と今後の順序は[実装進捗](IMPLEMENTATION-PROGRESS.md)にまとめています。

- 人口動態：市と18区の2000〜2025年、市月別2000-01〜2026-07。10指標・8,130値。出生・死亡、市外/市内転出入、その他、自然/社会/総増減を区別します。
- 年齢構成：各年1月1日の推計人口、2000〜2025年、市と18区。3区分・年齢不詳・総数の5指標・2,470値。割合の分母は不詳を含む総人口です。
- `/population-movement/`、市・18区の内訳ページ、`/age-structure/`、入口・検索・出典、CSV/JSON、既存5つのMCPツールへ接続しました。
- `pnpm verify` は48単体テスト、型・書式、原本照合、52ページのビルドが合格。PC/スマホのE2E46件、ローカルMCP実接続、3テーマの自動アクセシビリティ検査、320px幅の確認も合格しました。
- 原本の再取得は両系列とも `unchanged`。原本改変・年全体の削除・出典行偽装を拒否する一時環境の公開検査を追加しました。直接のAstroビルドとMCP公開にも数値検査を接続しています。
- 既存3記事は、書式変更を検出した初回検証の停止後、固定した最終版で3工程を再実行し33主張を受理しました。新規画面全体がAI検証済みという意味ではありません。
- 独立担当 `dynamics_review` が両系列を読み取りレビューし、年齢値2,470件を原典行から別処理で照合しました。表の行見出し欠落を修正し、JavaScript無効時の回帰テストを追加しました。最終版と本番反映の記録は以下のとおりです。

WebとMCPへの本番反映・公開URLでの確認まで完了しました。市民テスト、専門家監修、国内都市比較、リデザインの画面改修は未実施です。

### 最終レビュー

実装commitは `c9c8f93736406accafa50a70737c4baa0a33d75c`。独立担当 `/root/dynamics_review` がこの版を読み取り確認し、重大・中程度の未解消指摘なしと報告しました。最終確認はコードレビューで、全テストの再実行は実装担当によるものです。
文書内のローカルリンク45件、依存関係監査（既知の脆弱性0）も確認しました。原本CSVはCRLFを含め取得時のまま保存しています。

### 本番反映

- 公開対象commit: `c1665174af8a1969fe04f05f195807cb38b85ee5`。実装は独立レビュー対象と同一で、追加差分はレビュー記録です。
- [GitHub CI](https://github.com/toyofukux/open-yokohama/actions/runs/34005823981) 全ジョブ成功後に公開しました。Web/MCPともdry-runを通過しました。
- Web version: `fc569455-f39d-4dad-a1ed-d862a2afc69d`。
- MCP version: `1fe08a9b-8243-4330-a393-7294feabe7f4`。
- 本番WebでPC/スマホE2E46件合格。本番MCPへSDKで実接続し、既存・追加指標、出典、時系列、18区、入力制限を確認しました。
- 本番の `population.json`・`dynamics.json`・`ages.json` は、ビルド成果物とバイト単位で一致しました。
- 実行ログはローカルの `artifacts/demography-production-*` に保存。開発サーバーは停止済みです。

## 2026-09-06: リデザインの調査と計画

- デジタル庁・GOV.UK・ONS・W3Cの一次資料と既存実装を確認し、[リデザイン計画](REDESIGN-PLAN.md)を作成しました。
- 独自の情報構造・文字・図表・部品の設計、昼夜・期間テーマの互換条件、市民検証、段階別の受入条件を整理しました。
- 計画案の作成のみです。画面改修・本番反映・市民調査・独立レビュー・今回のアプリテストは未実施です。
- 次の着手単位は計画の段階0〜2です。現状の画面・性能の基準を作り、トップ・区詳細・比較のデザイン2案を用意します。

更新: 2026-09-06。現在は**人口・世帯の公開β版**です。[長期構想](VISION.md)のv1全体と市民利用目標は未達です。

## 公開先

- Web: https://open.yokohama
- MCP: https://open-yokohama-mcp.toyofukux.workers.dev/mcp
- OSS: https://github.com/toyofukux/open-yokohama

以下の過去の記録には変更前の名称・URLが含まれます。現在の接続状態は末尾の最新記録をご確認ください。

## 実装済み

- 計画・設計判断、段階別の受入条件、費用抑制方針。
- 公式人口CSVの32か月、9指標、横浜市+18区の5,472観測値。
- 原本不変保存、取得履歴、原典の行・列、参照日、改定番号、公開JSON/CSV。
- 欠測・重複・男女計・市区合計・密度・日付・原本ハッシュを検査するビルドゲート。
- 29の静的HTMLページ。トップ、18区詳細、比較、3つの解説、検索、出典、404等。
- 端末内検索、詳しさ切り替え、キーボード操作、スマホ表示、原本取得。
- MCP: 5つの読取ツール、都市/区リソース。公開JSONのみ参照し、LLM実行0。
- GitHub CI、週次更新検査ワークフロー、訂正テンプレート、MITとデータ帰属。

## 確認結果

- `pnpm verify`: lint、型、12件のテスト、原本照合、静的ビルドが合格。
- `pnpm test:e2e`: PC/スマホ計12件合格。主要4ページのWCAG A/AA自動検査で違反0。
- 内部リンク・アンカー1,319件の参照先を検査済み。
- `pnpm test:mcp`: ローカルworkerdと公開WorkerへSDKで実接続し、初期化・数値・時系列・18区・原典・Origin/サイズ制限を検証。
- 公式CSVの再取得: 同一内容は公開データを更新せず `unchanged` を返す。
- `pnpm audit`: 既知の脆弱性0（確認時点）。秘密パターン検査で検出0。
- スクリーンショットでPCとスマホの表示を確認。実装者による検証であり、独立レビューではありません。

- 公開Webでも12件のE2Eが合格。低速回線で比較選択が先行する不具合を修正し、回帰テストを追加しました。
- 公開MCPでもSDK接続検証に合格。
- GitHubをPUBLICに変更し、初期実装commit `d48e5fa2a06fee011b679ed78b9761b97b8f6682` をmainへpush済み。
- Web version: `e24a0a72-85f2-480f-9def-8254f99525e1`。
- MCP version: `b8830f57-3017-4f40-a54e-dac30c500dd2`。
- CI: https://github.com/toyofukux/yokohama-intelligence/actions/runs/33951787795 （初期実装commitで全ジョブ合格）。

## 残る開発・運用

1. 出生・死亡・転出入・年齢構成を取り込み、人口の理由を検証できるようにします。
2. 予算・決算・事業・成果を正しい会計/年度/政策単位で接続します。
3. 子育て・交通・防災等の課題を拡充。町丁・生活圏へ解像度を上げています。
4. AI文章生成→独立検証→人間承認の実フローを実装。現在あるのは契約と異常系テスト。
5. 市民10人の操作・理解テスト。8人以上が主要タスクを2分以内に完了し、重大な誤解0件が公開拡大条件。
6. 週次更新の実運用確認、訂正の担当体制、継続利用の測定、主要50指標・30課題への拡充。
7. 神戸・福岡との定義をそろえた比較と、公開基盤の長期構想の残項目。

人間の独立レビュー・市民利用テスト・WAU/再訪率は未実施です。実利用の成果を架空で計上しません。
公開Webは検証済みの公開データのみを参照します。

## 再開

正本: `docs/PLAN.md`。次の収集先と検証条件: `docs/DATA-ROADMAP.md`。数値更新: `docs/runbooks/data.md`。
外部参照repoは読み取りのみで変更していません。長期構想は[VISION.md](VISION.md)に整理しています。

## ローカル処理

実装中に使用した開発サーバーは作業終了時に停止します。再開はREADMEのコマンドを使います。

## 2026-09-05: 横浜の青と期間限定テーマ

- 基本配色を海の青・白・紺に変更。`packages/core/theme.ts`で通常配色・手動固定・期間を設定します。
- GREEN×EXPO 2027の開催期間だけ緑へ切り替え、終了時に青へ戻ります。静的配信のまま端末で判定します。
- URLの `?theme=yokohama` / `?theme=green-expo` でプレビューできます。
- `pnpm verify`合格（15件の単体テスト）。PC/スマホのE2Eは両配色・日時境界を含む18件で合格。
- Web version: `18af32de-f0d8-429a-8ed4-14dd92a27fc4`。
- 配色と運用手順: `docs/THEMES.md`。ドメイン10案: `docs/DOMAIN-CANDIDATES.md`。ドメインの選定・取得・DNS設定は未実施です。

## 2026-09-05: ドメイン手順と港の夜景

- ドメイン候補はユーザーが `open.yokohama` を優先。取得・DNS変更は未実施です。
- `docs/runbooks/domain.md` にPorkbun、Cloudflare Free、既存Workerへの接続、URL更新、MCP、確認・障害対応を記載しました。READMEからリンク済み。
- 横浜の座標で日の出・日没を端末内計算し、日没後から翌日の出まで港の夜景を適用。
- 濃紺の背景・金色の窓明かり・水面反射を追加。夜間のカード・フォーム・表・グラフも配色を統一。
- GREEN×EXPOは保留。期間設定を残し `enabled: false` に変更。公式素材は未使用です。
- `pnpm verify` 合格。その後追加した冬の照合・海外タイムゾーン検証も単体18件とE2E20件で合格。
- 公開環境でもPC/スマホ20件合格。夜景の実画面を両サイズで目視確認。
- 国立天文台の5日分との誤差3分未満を確認。演出用近似で、全日程の精度保証ではありません。
- Web version: `3cdbe1aa-9bc2-4a22-a1cd-2ed87d185092`。
- 追加の外部API通信・位置情報取得・サーバー処理なし。設定と制約は `docs/THEMES.md`。

## 2026-09-05: open.yokohama 接続準備（DNS競合で保留）

- Cloudflare APIでゾーンが正しいアカウントに属し `active` と確認。公開NSも指定値と一致。
- `wrangler.jsonc` に `open.yokohama` のCustom Domainを追加。接続はGitを正本に管理します。
- Astroの既定URL、サイトマップ、READMEを新URLに更新。旧 `workers.dev` は維持。
- `pnpm verify` 合格。更新後の旧公開URLでPC/スマホ20件のE2E合格。
- デプロイでWeb本体は更新されたが、Custom Domainはエラー100117で未接続です。新ドメインでのE2Eは未実施です。
- ユーザー確認の既存Aレコード2件（207.207.210.107 / 207.207.210.229）が接続に競合。
- 現在のWrangler認証はDNS一覧の取得が権限エラー。こちらからDNS削除はしていません。
- 再開: ユーザーが上記Aレコードを削除後、`pnpm exec wrangler deploy --config wrangler.jsonc`。
- 接続後は `TEST_BASE_URL=https://open.yokohama pnpm test:e2e`、canonical・sitemap・robots・HTTPSを確認してください。
- `www` とワイルドカードはPorkbun向けのまま。MCPの接続先も従来URLのまま。

## 2026-09-06: 独自ドメイン接続とOpen Yokohamaへの名称統一

- ユーザーによる旧Aレコード削除後、Gitの設定からCustom Domainを登録できました。
- WebのWorker名を `open-yokohama`、MCPを `open-yokohama-mcp` に変更しました。
- Cloudflare APIで `open.yokohama` の接続先が `open-yokohama` であることを確認しました。
- GitHubを `toyofukux/open-yokohama` に改名し、origin・リンク・プロジェクト説明・ホームページを更新しました。公開状態を維持しています。
- パッケージ名、MCPサーバー名、収集時のUser-Agent、画面・タイトルの表示名を統一しました。
- README・参加案内・セキュリティ・設計書・運用手順・Issueテンプレートを丁寧語に整えました。継続方針をCONTRIBUTING.mdに記載しました。
- 原資料は原文を保持しています。ローカル作業ディレクトリは既存のワークスペースパスを維持しています。
- `pnpm verify` が合格しました。新Workerによる `https://open.yokohama` でもPC/スマホ20件のE2Eが合格しました。
- 新MCPのSDK実接続検査も合格しました。HTTPS、ブランド、canonical、GitHubリンク、サイトマップ・robotsの新URLを確認しました。
- Web version: `3e61bcff-0267-4a27-860a-4132ca6c2195`。
- MCP version: `3b2db0d2-f015-493d-becb-487355dac104`。
- 旧Web・MCPのWorkerは旧URLの互換用として残しています。今後のデプロイ先は新Workerです。
- `www`・ワイルドカードのPorkbun向けCNAMEと、MCPの独自サブドメイン追加は今回変更していません。

## 2026-09-06: 公開基盤の構想整理

- 初期構想のうちOpen Yokohamaに関係する目的・設計原則・データモデル・公開Web/MCP・品質・長期目標を `docs/VISION.md` に整理しました。
- README・開発計画・現在の残作業・参加案内・セキュリティ・設計判断・MCP手順を公開基盤の範囲にそろえました。
- 初期構想の原文と公開基盤の対象外の記述をGit履歴から削除します。公開基盤の構想は `docs/VISION.md` を参照してください。
- 履歴の書き換えにより過去のcommit IDは変わります。上記の旧ID・CIリンクは当時の検証記録であり、書き換え後のcommitに対する再検証結果ではありません。

- 今回の文書整理では `pnpm verify`（単体18件・原本照合・ビルド）、`pnpm test:e2e`（20件）、文書内のローカルリンク22件の検査が合格しました。

## 2026-09-06: 説明の根拠と訂正経路（公開前検証）

既存3記事の主張と根拠を結び、AI検証と人間確認を区別する表示を実装しました。原典の版・本文・基準・検証実装に結び付いた公開検査をWebとMCPに接続しています。今回のAI検証は35主張で完了しました。一般的な精度や専門家による承認の実証ではありません。

数字・説明から箇所と版を添えてGitHubの報告フォームへ移動できます。比較画面の指標・時点も引き継ぎます。受付にはGitHubアカウントが必要です。サイト台帳は手動更新で、最新の受付・やり取りはGitHubが正本です。実在の報告がないため台帳は空です。

- `pnpm verify`: 型、書式、35単体テスト、原典照合、静的ビルドが合格しました。
- `pnpm test:e2e`: PC・スマホ計28件が合格しました。報告内容の外部遷移はテストで捕捉し、実際のIssueは作成していません。
- `pnpm test:corrections`: 一時チェックアウトで保留時の本文・HTML/OG見出し抑止、台帳の状態・理由・日付表示、Web/MCP共通一覧からの除外、未知の記事指定の拒否を確認しました。
- `pnpm test:factcheck-gates`: クリーン環境の原本再取得、未検証編集の各公開経路での拒否、原本改変の拒否を確認しました。
- MCPのWrangler dry-runとローカルworkerdへのSDK実接続が合格しました。依存関係監査で既知の脆弱性はありませんでした。
- 独立した担当 `factcheck_review` が訂正経路を読み取りレビューし、状態表示・保留見出し・報告文脈・保留対象の検査を修正しました。最終版の識別は後続記録に記載します。

残る点: アカウント不要の報告、GitHubとの自動同期、数値データ自体の一括取り下げ、分野別の専門家確認、法的な公開判断基準、未知資料での十分な精度評価です。記事の表示保留は再ビルド・公開後に反映されます。現在は説明記事単位で、既に取得されたデータや外部キャッシュを回収する機能ではありません。

次のコンテンツは人口増減の内訳です。運用手順は[訂正と表示保留](runbooks/corrections.md)、優先順位は[BACKLOG](BACKLOG.md)に記載しています。

### 最終版・公開状況

実装commit: `f2694a73456c0c635b8826fe1b90ec930a678de5`。独立担当 `/root/factcheck_review` がこの版を読み取り確認し、重大・中程度の未解消指摘なしと報告しました。実行検証は実装担当によるものです。

GitHubへのpushはOAuth認証に `workflow` scopeがないため拒否されました（CIファイルの更新を含みます）。権限を迂回せず、本番反映は未実施です。認証の権限追加後、push→CI結果確認→Web/MCP公開→本番導線確認を再開します。Web/MCPのdry-runは合格しています。

## 2026-09-06: 原典への直接リンクと表示の簡素化

認証追加後、先行実装のpushが成功し、CI `34003083926` が合格しました。
区詳細・比較表・解説の原典リンクをMaterialの資料アイコンへ変更しました。行番号は通常画面から除き、原典URLへ直接移動します。検証の詳細は「出典・確認状況」に折りたたみ、原典の行・列情報はデータに保持しています。

変更後の35主張のAI再検証、`pnpm verify`、PC・スマホ28件のE2Eが合格しました。独立担当 `factcheck_review` は表示変更を読み取り確認し、重大な表示・アクセシビリティ・リンク不整合なしと報告しました。アイコンの出典とライセンスは訂正運用手順に記載しています。

### 本番反映

- 公開コード: `9e8369aedbbcc9b0e906d701fb6fbe03968622a1`。
- Web version: `d6cdcf61-0d81-4879-a3af-8addf3a556b3`（https://open.yokohama）。
- MCP version: `09c9900e-1bbd-4eca-9763-0677ddf39ad1`。
- 本番WebでPC・スマホ28件のE2E、本番MCPへのSDK実接続が合格しました。GitHubへのテスト報告は送信していません。
- CI: https://github.com/toyofukux/open-yokohama/actions/runs/34003361508 （記録時点で実行中。ローカルの同版検査は合格済みです）。

認証による公開停止は解消しました。原典アイコン、検証詳細の折りたたみ、箇所・版付きの訂正報告、訂正一覧を公開済みです。専門家監修・法的な確認が完了したという意味ではありません。

## 2026-09-06: TOPページの左右端をヘッダに統一

- TOPページの801px以上の表示で、メインの最大幅を1160pxからヘッダと同じ1240pxに変更しました。スマホの左右20pxの余白は維持しています。
- 実装commit: `b492595`（`small-fix`ブランチ）。ユーザーの本番反映許可を受け、`https://open.yokohama` に公開しました。
- Web version: `8fc059a1-b23d-4587-a38d-6fc281482aa9`。直前の版: `3e61bcff-0267-4a27-860a-4132ca6c2195`。
- `pnpm verify`、ローカルと公開環境のE2E各20件が合格しました。公開環境では375・390・800・801・1024・1280・1440・1920pxの幅でヘッダとメインの左右端の一致、横溢れなしを確認しました。
- 公開コマンドは `SITE_URL=https://open.yokohama pnpm run deploy` です。`pnpm deploy` はpnpm自身のサブコマンドと競合するため、`run`が必要でした。

## 2026-09-06: ローカル作業のmain統合とworktree整理

- `small-fix` の2コミット（`b492595`、`576ff94`）と、`design` の未コミット文書を保存した `4be5997` を統合しました。統合版は `c5cbadc` です。
- 公開記録の競合は両方の記録を保持して解消しました。リデザインは計画案の保存であり、全画面改修の実施記録ではありません。
- 統合版で `pnpm verify`（単体35件、型・書式・原典照合・ビルド・内部リンク705件）とPC/スマホのE2E28件が合格しました。計画書のローカルリンク4件も確認しました。
- 今回の整理では本番への再デプロイは行っていません。上記の公開記録は各作業時点の履歴です。
- GitHubのmainへのpushとCI確認後、統合済みの補助worktreeとブランチを削除します。

## 2026-09-06: 資料アイコンを横浜市の掲載ページへ変更

公開コード `1dccade`。区詳細・比較・解説の資料アイコンは横浜市のHTML掲載ページへ移動します。アイコンの読み上げ名は「横浜市の掲載ページを開く」です。詳細欄に残すCSVリンクは「CSVダウンロード」と明示しました。

35主張の再検証、`pnpm verify`、ローカル・本番それぞれPC/スマホ28件のE2E、本番MCPのSDK実接続が合格しました。リンク先がCSVではなく横浜市のHTMLであることも確認しました。

- Web version: `e83972c1-edc8-42cc-8b58-136d66a93107`
- MCP version: `52eed27e-2b2e-4913-b221-872c5862e4ab`

GitHubへpushし、本番反映済みです。
